# rnad-requests

`request-analyzer` is a CLI tool for analyzing AWS CloudWatch logs using GoAccess for real-time log visualization.

## Installation

1. Clone the repository:
   ```bash
   git clone https://github.com/rnadjqaim/request-analyzer.git
   cd request-analyzer
